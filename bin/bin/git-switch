#!/bin/bash

case "$1" in
'private')
git config --global user.name "Tormod Erevik Lea"
git config --global user.email tormodlea@gmail.com
;;
'laerdal')
git config --global user.name "Tormod Erevik Lea"
git config --global user.email tormod.lea@laerdal.com
;;
*)
git config --global user.name "Tormod Erevik Lea"
git config --global user.email tormodlea@gmail.com
;;
esac

git config --global user.name
git config --global user.email
